version: '3.8'
services:

  app:
    build:
      context: .
      target: development
    ports:
      - "4200:4200"

    command: npm start
    volumes:
      - .:/app
      - node_modules:/app/node_modules      

volumes:
  node_modules:
    